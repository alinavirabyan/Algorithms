# Palindrome Generator

This project generates a **palindrome** from the characters of a given string.

The program counts the frequency of each character and uses those counts to construct the first half of the palindrome. Characters with odd frequencies are handled separately and placed in the middle when possible.

**Main Concept:** Character Frequency and Palindrome Construction

**Time Complexity:** O(n)

def new_function():
    for i in range(len(arr) * len(arr)):
        x = i // len(arr)
        y = i % len(arr)
        if arr[x] + arr[y] == target:
            print(x)
            # print(y)

arr = [0, 7, 11, 2]
target = 9
new_function()
